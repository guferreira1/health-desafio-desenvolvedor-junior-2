import styled from "styled-components";
import { Colors } from "../../themes/themeColors";

export const PetStyles = styled.main`
  display: flex;
  flex-direction: column;
  background-color: ${Colors.primary};
  gap: 20px;
  width: 100%;
  height: 100vh;
`;
